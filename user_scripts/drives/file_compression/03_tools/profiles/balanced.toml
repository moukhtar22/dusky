# Balanced — default JC l7, 64M, zstd22, nilsimsa (recommended)
level = 7
block_size_bits = 24
max_lookback = 26
order = "nilsimsa"
compression = "zstd:level=22"
note = "Default, best speed/ratio for games"
