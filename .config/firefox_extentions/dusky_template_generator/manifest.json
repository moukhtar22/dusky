{
  "manifest_version": 2,
  "name": "Dusky Template Generator",
  "version": "2.0.0",
  "description": "Author per-site CSS theme templates for Dusky Sites: map a site's colour variables to your Matugen palette, or pick elements visually.",
  "browser_specific_settings": {
    "gecko": {
      "id": "dusky_template_generator@dusk.com",
      "strict_min_version": "128.0",
      "data_collection_permissions": { "required": ["none"] }
    }
  },
  "permissions": [
    "activeTab",
    "nativeMessaging"
  ],
  "background": {
    "scripts": ["background.js"],
    "persistent": false
  },
  "browser_action": {
    "default_title": "Dusky Template Generator — theme this site",
    "default_popup": "popup.html"
  },
  "commands": {
    "toggle-picker": {
      "suggested_key": { "default": "Alt+Shift+P" },
      "description": "Start / stop the visual element picker on the current page"
    }
  }
}
