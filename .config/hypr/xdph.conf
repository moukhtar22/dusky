# ~/.config/hypr/xdph.conf
# This is XDPH config, not Hyprland config. Restart portal after edit.

screencopy {
    # 0 = no limit, 60 saves battery, 120 is default
    max_fps = 60

    # false = ask every time (safer), true = auto-check restore token box
    allow_token_by_default = false
}
